# 能力勋章 · 勋章组参考文档

> 本文档根据「能力勋章」mod 源码（`能力勋章/scripts/medal_defs/functional_medal_defs.lua`）整理，供本 mod 开发对照使用。
> 更新于：2026-08-20

---

## 一、勋章总览

### ✅ 已完成

| 组 | 勋章 |
|---|---|
| 伐木 chopMedal | 初/中/高级：small/medium/largechop_certificate |
| 矿工 minerMedal | 初/中/高级：small/medium/largeminer_certificate |
| 烹饪 chefMedal | 烹饪/大厨/主厨：cook/chef/headchef_certificate |
| 巧手 handyMedal | 巧手考验→巧手：handy_test→handy_certificate |
| 丰收 harvest_certificate | 丰收（单枚） |
| 智慧 wisdomMedal | 蒙昧考验→智慧：wisdom_test→wisdom_certificate |
| 植物 plantMedal | 虫木→植物：plant→transplant_certificate |
| 时空 speedMedal | 速度/空间/时空：speed/space/space_time_certificate |
| 钓鱼 fishingMedal | 钓鱼/垂钓/渔翁：small/medium/largefishing_certificate |
| 童真 childMedal | 童心→童真：childishness→childlike_certificate |
| 暗影 shadowmagicMedal | 暗影（单枚） |
| 淘气 naughtyMedal | 淘气（单枚） |
| 浴火 bathfireMedal | 浴火（单枚） |
| 正义 justiceMedal | 逮捕→正义：arrest→justice_certificate ⚙自动补充正义值 |
| 女武神 valkyrieMedal | 检验→考验→女武神：valkyrie_examine→valkyrie_test→valkyrie_certificate（UI显示"女武神◆"） |
| 触手 tentacle_certificate | 触手（单枚） |
| 传承 inherit_certificate | 传承（单枚，标签按等级动态赋予） |
| 羽绒 down_filled_coat_certificate | 羽绒服（单枚）⚙自动补充耐久 |
| 蓝晶 blue_crystal_certificate | 蓝晶（单枚）⚙自动补充耐久 |
| 复眼 ommateum_certificate | 复眼（单枚）⚙自动补充耐久 |
| 踏水 treadwater_certificate | 踏水（单枚）⚙自动补充耐久 |

### ⏳ 未完成

| 组 | 勋章 |
|---|---|
| 友善 friendlyMedal | 友善→挚友：friendly→bosom_friend_certificate |
| 蜘蛛 spiderMedal | 蜘蛛（单枚） |
| 噬魂 devoursoulMedal | 噬灵→噬魂→噬空 |
| 鱼人 merm_certificate | 鱼人（单枚） |
| 沉默 silence_certificate | 沉默（单枚） |
| 蜂王 bee_king_certificate | 蜂王（单枚） |


---

## 二、各已完成组的详细动作配置

### chopMedal（伐木）

| 动作 | 条件 | 说明 |
|---|---|---|
| `CHOP` | 排除 `burnt` 标签 | 砍焦树不触发（能力勋章砍焦树不消耗） |
| `DIG` | 要求 `stump` 标签 | 仅挖树桩触发 |

### minerMedal（矿工）

| 动作 | 条件 | 说明 |
|---|---|---|
| `MINE` | 排除 `rock_avocado_fruit` | 挖石果不触发（能力勋章消耗0） |

### chefMedal（厨师）

**无条件动作（action_ids）**：

| 动作 | 依赖 | 备注 |
|---|---|---|
| `COOK` | seasoningchef | 批量烤制；红晶锅整组烹饪需本源+主厨 |
| `MURDER` | masterchef | 快速杀生 |
| `CHEFFLAVOUR` | seasoningchef | 调味，整组需本源勋章 |
| `MEDAL_GRINDING` | seasoningchef | 研磨，整组需时空勋章 |
| `MEDAL_EAT_SPICES` | seasoningchef+本源 | 吃调料粉 |
| `MAKECOOLDOWN` | expertchef | 强制冷却 |
| `MEDALPOLLUTE` | seasoningchef | 污染血糖 |

**带条件动作（action_targets）**：

| 动作 | 条件 | 说明 |
|---|---|---|
| `HARVEST` | `has_component=stewer` | 从锅收获料理（升级） |
| `RUMMAGE` | `prefabs=portablecookpot/portablespicer` | 打开便携烹饪锅/调料站 |
| `OPEN_CRAFTING` | `prefabs=portableblender` | 打开便携搅拌器 |
| `DEPLOY` | `prefabs=..._item` | 展开便携设备 |
| `DISMANTLE` | `prefabs=portablecookpot/blender/spicer` | 收回便携设备 |
| `BUILD` | `recipe_builder_tag=masterchef/professionalchef/seasoningchef` | 制作厨师专属配方 |

> **注意**：chefMedal 是通过事件/能力升级（收获料理），但有大量专属动作（调味/研磨/制作/使用厨具），因此自动装备覆盖了这些专属动作。
> 只有 `chopMedal` 和 `minerMedal` 是通过 `finishedwork`（挥砍/挖掘动作）消耗耐久的组。

### handyMedal（巧手）

**带条件动作（action_targets）**：

| 动作 | 条件 | 说明 |
|---|---|---|
| `BUILD` | `exclude_recipe_props=builder_tag`；`keep_recipe_builder_tag=handyperson/has_handy_medal` | 制作所有东西（快速制作），排除其他勋章专属配方，保留女工+巧手专属 |
| `UNWRAP` | `has_component=unwrappable` | 快速拆包 |
| `UNWRAPGIFTFRUIT` | `prefabs=medal_gift_fruit` | 拆包果（本源+巧手批量5个） |
| `UNWRAPOVERSIZEDGIFTFRUIT` | `all_tags=oversized_veggie+waxable` | 拆巨型包果（本源+巧手专属） |
| `DEPLOY` | `prefabs=winona_catapult_item/winona_spotlight_item/winona_battery_low_item/winona_battery_high_item` | 部署女工专属建筑（投石机/探照灯/发电机） |
| `DISMANTLE` | （暂未启用） | 拆除女工专属建筑 |

### harvestMedal（丰收）

**无条件动作（action_ids）**：

| 动作 | 依赖 | 备注 |
|---|---|---|
| `MEDAL_QUICK_DRY` | medal_fastpicker | 晾肉架一键晾干 |
| `MEDAL_FASTPICK_MEATRACK` | medal_fastpicker | 晾肉架快采 |
| `HARVEST` | medal_fastpicker | 快速收获（无条件） |

**带条件动作（action_targets）**：

| 动作 | 条件 | 说明 |
|---|---|---|
| `PICK` | 排除 `noquickpick` 标签 | 快速采摘；排除大垃圾堆等不能快采的目标 |
| `TAKEITEM` | `tags=inventoryitemholder_take/takeshelfitem` | 仅持有器/架子有物可取时快采取物 |

**PICK 跨组优先级（植物勋章 > 丰收勋章）**：PICK 同时被植物勋章组（采巨型作物/带刺植物）与丰收勋章组命中时，只装备优先级最高者。`transplant_certificate`(60) / `plant_certificate`(60) > `harvest_certificate`(40)。

> 丰收勋章为单枚勋章（无 grouptag，`medal_fastpicker` 快采），本源加成已放开。

### plantMedal（植物/虫木）

**无条件动作（action_ids）**：

| 动作 | 依赖 | 备注 |
|---|---|---|
| `MEDALTRANSPLANT` | has_transplant_medal | 月光移植 |
| `MEDALNORMALTRANSPLANT` | has_transplant_medal | 普通移植 |
| `GIVEROOTCHESTLIFE` | has_plant_medal | 树根宝箱复苏 |
| `MAKEMUSHTREE` | has_transplant_medal | 变蘑菇树（本源勋章可绕过月光限制） |
| `MAKEMANDRAKEPLANT` | has_transplant_medal | 复活曼德拉（本源勋章可绕过月光限制） |
| `GRAFTING_TREE` | has_transplant_medal | 嫁接 |
| `PUT_IN_THE_SEEDS` | has_transplant_medal | 塞入种子 |
| `MEDAL_FRUIT_TREE_LEVEL_UP` | has_transplant_medal | 嫁接树升级 |
| `MEDALMOONTREEHARVEST` | 本源+植物勋章 | 采摘月树花 |
| `MEDALTREEROCKSHARVEST` | 本源+植物勋章 | 采摘巨石枝 |
| `PLANTSOIL` | plantkin | 种田 |

**带条件动作（action_targets）**：

| 动作 | 条件 | 说明 |
|---|---|---|
| `BUILD` | `recipe_builder_tag=has_plant_medal/has_transplant_medal` | 制作植物专属配方（月光权杖/肥料包/月光锤/月光网/月光法杖等） |
| `DEPLOY` | `hand_tags=deployedfarmplant` | 种下农场作物种子（手持种子带标签） |
| `PICK`（按勋章分组） | 见下 | 采带刺植物→植物勋章；采巨型作物→虫木勋章 |
| `HARVEST` | `prefabs=waterplant` | 收获藤壶（戴植物勋章带 plantkin 免被海草攻击） |

**HARVEST 跨组优先级（植物勋章 > 丰收勋章）**：HARVEST 同时被植物勋章组（收获藤壶）、厨师组（从锅收获）与丰收勋章组（无条件快收）命中时，只装备优先级最高者。`transplant_certificate`(60) / `plant_certificate`(60) > `cook_certificate`/`chef_certificate`(50) > `harvest_certificate`(40) > `headchef_certificate`(30)。

**PICK 按勋章分组**（同一动作不同勋章分别限制）：

```lua
PICK = {
    transplant_certificate = { tags = { "thorny" } },            --采带刺植物(thorny)→戴植物勋章
    plant_certificate = { props = { is_oversized = true },
                          exclude_tags = { "farm_plant_killjoy" } }, --采巨型作物(is_oversized)→戴虫木勋章，排除腐烂作物
},
```

- **带刺植物（`thorny`）**：玫瑰、仙人掌、荆棘、刺灌木等 → 自动装备**植物勋章**
- **巨型作物（`is_oversized` 属性，非标签）**：农场作物植株长成巨型 → 自动装备**虫木勋章**（帮虫木升级），排除腐烂作物（`farm_plant_killjoy`）
- `is_oversized` 是农场作物植株的属性（`farm_plants.lua:518`），不是标签，用 `props` 字段判断

### wisdomMedal（智慧）

**带条件动作（action_targets）**：

| 动作 | 条件 | 说明 |
|---|---|---|
| `BUILD` | `recipe_builder_tag=wisdombuilder/bookbuilder` | 制作陷阱重置册（智慧专属配方）或原版书籍/书桌 |
| `READ` | `tags=bookcabinet_item`，排除 `simplebook`/`closed_book` | 读需要 reader 的书（原版书籍/新华字典/勋章书籍） |

- **READ 排除**：烹饪书（`simplebook`，普通角色就能读）；无字天书（`closed_book`，走 READ 但消耗蒙昧勋章耐久，非智慧勋章）。
- **蒙昧勋章（`wisdom_test_certificate`）不参与自动装备**，改为**自动答题**（见第三节 §5）。

### speedMedal（速度/时空）

**无条件动作（action_ids）**：

| 动作 | 依赖 | 备注 |
|---|---|---|
| `MEDAL_GRINDING` | spacetime_medal | 研磨增强（跨组优先级主厨>时空>巧手） |
| `MEDALFEEDBIRD` | spacetime_medal | 整组喂鸟（需本源+时空） |
| `MEDALDELIVERYTREASURE` | spacetime_medal | 时空符文寻宝 |

**带条件动作（action_targets）**：

| 动作 | 条件 | 说明 |
|---|---|---|
| `BUILD` | `recipe_builder_tag=spacetime_medal` | 制作时空专属配方（琥珀灵石/预言水晶球/改命药水/时空符文/时空尘蛾窝等） |
| `READ` | `prefabs=unsolved_book` + `season_fish` | 阅读未解之谜书献祭季节鱼换季（周围献祭范围有季节鱼且当前季节≠对应季节才触发） |
| `ATTACK` | `slingshot_ammo=medalslingshotammo_sandspike` | 弹弓装沙刺弹攻击（佩戴时空勋章无视地形生成时空之刃） |

**跨组优先级**：`MEDAL_GRINDING`（主厨30 > 时空20 > 巧手10）；`READ`（智慧20 > 时空10）。

### childMedal（童真）

**无条件动作（action_ids）**：

| 动作 | 依赖 | 备注 |
|---|---|---|
| `TELLSTORY` | storyteller | 讲故事（无条件） |

**带条件动作（action_targets）**：

| 动作 | 条件 | 说明 |
|---|---|---|
| `EQUIP` | `tags=slingshot` | 装备弹弓（含皮肤/变体版） |
| `ATTACK` | `slingshot_ammo=tag:slingshotammo` | 弹弓射击任意弹药（统一 tag，消耗耐久） |
| `BUILD` | `recipe_builder_tag=pebblemaker/slingshot_sharpshooter/pinetreepioneer/troublemaker/has_childishness/senior_childishness` | 制作童真相关配方（6个童真标签全覆盖） |

> **`slingshot_ammo` 支持 tag 写法**：条件项以 `tag:` 开头时（如 `tag:slingshotammo`），判断弹弓弹药槽第1格的弹药物品是否带该 tag（`HasTag`），而非逐个 prefab 匹配 `projectile`。所有能放入弹弓的弹药（原版/童真专属/融化大理石/曼德拉果）都带 `slingshotammo` tag。

**ATTACK 跨组优先级（童真 > 时空）**：`ATTACK` 同时被童真组（弹弓射击）与时空组（沙刺弹时空之刃）命中时，`childlike_certificate`/`childishness_certificate`(60) > `space_time_certificate`(50)。

### shadowmagicMedal（暗影）

**无条件动作（action_ids）**：

| 动作 | 依赖 | 备注 |
|---|---|---|
| `USESPELLBOOK` | magician | 使用魔法书 |
| `CASTAOE` | magician | 施放范围魔法 |
| `USEMAGICTOOL` | magician | 使用魔法工具 |

**带条件动作（action_targets）**：

| 动作 | 条件 | 说明 |
|---|---|---|
| `EQUIP` | `prefabs=sanityrock_mace` | 装备方尖锏 |
| `BUILD` | `recipe_builder_tag=has_shadowmagic_medal` | 制作暗影魔法工具 `medal_shadow_tool` |

### naughtyMedal（淘气）

**带条件动作（action_targets）**：

| 动作 | 条件 | 说明 |
|---|---|---|
| `PLAY` | `prefabs=medal_naughtybell` | 摇淘气铃铛 |

### fishingMedal（钓鱼）

**无条件动作（action_ids）**：

| 动作 | 依赖 | 备注 |
|---|---|---|
| `FISH` | — | 陆地钓鱼（享受咬钩加速等加成） |
| `OCEAN_FISHING_CAST` | — | 海洋钓鱼投竿 |
| `OCEAN_FISHING_REEL` | — | 海洋钓鱼收线 |

**带条件动作（action_targets）**：

| 动作 | 条件 | 说明 |
|---|---|---|
| `MURDER` | `tags=fish` | 快速杀鱼（目标带 `fish` 标签） |
| `BUILD` | `recipe_builder_tag=has_largefishing_medal` | 制作渔翁专属配方（特制鱼食 `medal_chum`） |

**MURDER 跨组优先级（主厨/大厨 > 渔翁/垂钓）**：`MURDER` 同时被厨师组（无条件快速杀）与钓鱼组（杀鱼）命中时，`headchef_certificate`/`chef_certificate`(20) > `largefishing_certificate`/`mediumfishing_certificate`(10)。

### bathfireMedal（浴火）

**无条件动作（action_ids）**：

| 动作 | 依赖 | 备注 |
|---|---|---|
| `COOK` | expertchef | 快速烹饪（`domediumaction`） |
| `MAKECOOLDOWN` | expertchef | 强制冷却（对带 `blueflame`+`fire` 标签目标或 `staffcoldlight`） |
| `SMOTHER` | pyromaniac | 灭火（常规灭火，目标带 `smolder` 标签闷烧；pyromaniac 加速） |
| `MANUALEXTINGUISH` | pyromaniac | 手动灭火（需手持冻结物 `frozen`，目标带 `fire` 燃烧；pyromaniac 加速） |
| `LIGHT` | pyromaniac | 点火（原版无标签限制，pyromaniac 加速动作） |

**带条件动作（action_targets）**：

| 动作 | 条件 | 说明 |
|---|---|---|
| `BUILD` | `recipe_builder_tag=has_bathfire_medal/pyromaniac` | 制作浴火专属配方/打火机（`lighter`）/伯尼熊（`bernie_inactive`） |
| `ADDFUEL` | `tags=campfire/prefab:nightlight` | 给营火类（`campfire` 标签）或夜灯（`prefab:nightlight`）加燃料（`fuelmaster` 燃烧效率加成，对齐能力勋章浴火勋章的加成范围） |
| `EQUIP` | `prefabs=armor_medal_obsidian/armor_blue_crystal/armor_medal_space_time` | 装备红晶甲/蓝晶甲/时空晶甲（本源+浴火反伤加成） |

**COOK/MAKECOOLDOWN 跨组优先级（主厨/大厨 > 浴火）**：`COOK`/`MAKECOOLDOWN` 同时被厨师组与浴火组命中时，`headchef_certificate`/`chef_certificate`(20) > `bathingfire_certificate`(10)。

> 浴火勋章为单枚勋章（无 grouptag），本源加成已放开。`bernieowner` 标签为纯被动能力，本 mod 不赋予。

### justiceMedal（正义）

逮捕→正义升级链（`arrest_certificate` → `justice_certificate`），逮捕优先（`Groups` 顺序靠前），本源加成已放开。

**带条件动作（action_targets）**：

| 动作 | 条件 | 说明 |
|---|---|---|
| `ATTACK` | `arrest_certificate={prefabs=krampus/medal_naughty_krampus}` | 攻击坎普斯/复仇坎普斯戴**逮捕勋章**（击杀消耗耐久升级为正义） |
| | `justice_certificate={tags=epic/monster/norewardtoiler/prefab:lightninggoat/prefab:tentacle_pillar}` | 攻击其他怪物/特定目标戴**正义勋章**（获得正义值/触发特殊掉落） |

> 同组内逮捕优先级高：攻击坎普斯时逮捕分组优先（`MEDAL_LEVELS` 组内对比）。**指定勋章未持有则不装备**（三层对比第一层剔除未持有，不再回退组内最优）。

**自动补充正义值**（见第三节 §7）：正义勋章入 `AUTOREPAIR_MEDALS`，默认**智能**模式，攻击目标时按目标自动补足正义值。

### valkyrieMedal（女武神◆）

检验→考验→女武神升级链（`valkyrie_examine_certificate` → `valkyrie_test_certificate` → `valkyrie_certificate`），UI 显示名"女武神◆"。最终女武神勋章（`Groups` 顺序靠前，`MEDAL_LEVELS` 最高），本源加成已放开。

**带条件动作（action_targets）**：

| 动作 | 条件 | 说明 |
|---|---|---|
| `BUILD` | `valkyrie_certificate={recipe_builder_tag=valkyrie}` | 制作女武神专属配方（女武神之矛 `spear_wathgrithr`/头盔 `wathgrithrhat`，`builder_tag=valkyrie`）时装最终女武神勋章（提供 `valkyrie` 标签解锁配方） |
| `ATTACK` | 按勋章分组 | 见下（三等级各自目标限制，防升级失败） |

**ATTACK 按勋章分组**（攻击目标与佩戴勋章匹配，三等级独立限制）：

```lua
ATTACK = {
    valkyrie_examine_certificate = {
        { all_tags = { "largecreature", "monster" } },  --大型怪物(须同时带largecreature+monster)
        { tags = { "epic" } },                            --或epic生物
    },            --检验：攻击大型怪物/epic，消耗耐久升级(对齐能力勋章 IsValidVictim)
    valkyrie_test_certificate = {
        tags = { "monster", "epic" },          --epic或monster
        exclude_tags = { "smallcreature" },    --排除小动物
    },            --考验：攻击epic或monster且非小动物，消耗耐久升级
    valkyrie_certificate = {
        exclude_tags = { "veggie", "structure", "wall", "balloon", "groundspike",
            "smashable", "abigail", "shadowminion", "companion" },  --排除IsValidVictim的9项
    },            --女武神：攻击有效目标(非植物/建筑/墙/气球/尖刺/可砸碎/阿比盖尔/暗影随从/随从)，回血/减伤
}
```

> 女武神勋章（最终级）已满级，攻击不匹配目标不会降级，其目标为 `IsValidVictim` 排除9项；检验/考验是升级中勋章，需严格匹配目标（见 `helper_attack_block.lua` 攻击拦截）。

**ATTACK 跨组优先级**（`CROSS_GROUP_PRIORITY.ATTACK`）：
- 童真/童心 `childlike/childishness_certificate`(60) > 时空 `space_time_certificate`(50) > 考验 `valkyrie_test_certificate`(40，默认武神) > 逮捕/正义 `arrest/justice_certificate`(30) > 女武神 `valkyrie_certificate`(20) > 触手 `tentacle_certificate`(10)。
- **正义武神模式**（UI「正义武神」开关，默认**武神**）：选"正义"时考验/检验跨组优先级动态降为 20（女武神中级勋章低优先，正义组优先）。

### tentacleMedal（触手）

单枚最终勋章（`tentacle_certificate`，能力勋章无显式 grouptag），本源加成已放开。佩戴时赋予 `tentaclemedal` 标签（解锁配方制作/活性触手尖刺融合），攻击召唤触手是勋章**装备时**通过 `onattackother` 监听生效。

**无条件动作（action_ids）**：

| 动作 | 依赖 | 备注 |
|---|---|---|
| `MEDALSPIKEFUSE` | tentaclemedal | 活性触手尖刺融合（需手持/目标均为 `medal_tentaclespike`） |
| `ATTACK` | tentaclemedal | 攻击时自动装备触手勋章（召唤触手），对齐能力勋章原版**无目标限制**（`onattackotherfn` 只要求有武器+勋章等级>0） |

**带条件动作（action_targets）**：

| 动作 | 条件 | 说明 |
|---|---|---|
| `BUILD` | `recipe_builder_tag=tentaclemedal` | 制作触手专属配方：狼牙棒 `tentaclespike`、活性触手尖刺 `medal_tentaclespike` |
| `EQUIP` | `prefabs=medal_tentaclespike` | 装备活性触手尖刺时自动装备触手勋章（享受按勋章等级/本源加成的攻击范围） |

> 触手勋章为单枚勋章（无 grouptag），本源加成已放开。`senior_tentaclemedal`（高级免疫触手攻击）为被动能力，本 mod 不赋予。

### inheritMedal（传承）

单枚可升级勋章（`inherit_certificate`，能力勋章无显式 grouptag，`upgradable=true` 且 `maxlevel=3`）。佩戴时按等级赋予 `traditionalbearer1..N` 标签（对齐原版 `onequipfn`），解锁对应等级配方。

**标签按等级动态赋予（`level_tag_base`）**：
- `helper_rules.lua` 配置 `level_tag_base = "traditionalbearer"`（非固定 tags）
- `helper_tags.lua` 拥有且未佩戴时，遍历持有的传承勋章实例取**最高 `medal_level`**，动态赋 `traditionalbearer1..最高level`
- 可升级勋章初始等级恒为 1（`functional_medals.lua:175`），故 1 级至少赋 `traditionalbearer1`

**配方按等级解锁**（每个等级的 `traditionalbearerN` 各解锁一部分配方，非最高级解锁全部）：
- `traditionalbearer1`：羽绒帽 `down_filled_coat`、蓝晶帽 `blue_crystal`
- `traditionalbearer2`：复眼勋章 `ommateum_certificate`、速度勋章 `speed_certificate`
- `traditionalbearer3`：空间勋章 `space_certificate`、未解之谜、本源精华 `medal_origin_essence` 等

**带条件动作（action_targets）**：

| 动作 | 条件 | 说明 |
|---|---|---|
| `BUILD` | `recipe_builder_tag=traditionalbearer1/2/3` | 制作传承勋章解锁的任意等级配方时自动装备传承勋章（必须写全三个等级标签，因为配方精确匹配各自 `builder_tag`） |

> 传承勋章为单枚勋章（无 grouptag），本源加成未放开（无本源特殊效果）。

---

## 三、本 mod 核心机制

> 以下为 `scripts/` 下各模块的职责与关键逻辑，供开发维护对照。

### 1. 临时标签/组件同步（`helper_tags.lua`）

- 拥有勋章但**未佩戴**时，临时赋予 `helper_rules.lua` 配置的**标签**和**组件**，使专属能力可用。规则文件支持 `tags`、`components` 与 `level_tag_base` 三字段（`helper_rules.lua` 有注释说明）。
- **`level_tag_base`（按等级动态赋标签）**：如传承勋章 `level_tag_base="traditionalbearer"`，`helper_tags.lua` 拥有且未佩戴时遍历持有的勋章实例取最高 `medal_level`，动态赋 `base1..base最高level`（对齐原版 `onequipfn`），避免低等级解锁高级配方。
- **标签 vs 组件**：部分能力（`reader`/`storyteller`/`magician`）原版判定需对应**组件**（如 `ACTIONS.READ` 查 `components.reader`），仅赋标签会导致动作显示但无法真正执行，故这些走 `components` 字段；组件构造会自动带上同名标签（组件+标签一体），与 `HasTag` 判定兼容。
- **佩戴**后（含融合勋章内部），临时项归能力勋章管，本 mod 不误删。
- 每次勋章状态变化（`itemget/itemlose/equip/unequip`）通过 `QueueMedalRefresh` **下一帧合并**（防抖）只刷新一次，避免连发事件频繁全量扫描。

**引用计数对齐（关键）**：
- 标签/组件的赋予与移除复用能力勋章的计数函数 `GLOBAL.AddMedalTag/RemoveMedalTag`、`GLOBAL.AddMedalComponent/RemoveMedalComponent`（`medal_globalfn.lua` 已暴露）。
- 作用：mod 临时 1 份 + 佩戴 1 份 → 卸载能力勋章减 1 → mod 清理再减 1 → 减到 0 才真正 `RemoveTag`/`RemoveComponent`。

**原生组件/标签保护（对应角色天生能力）**：
- 各勋章能力对应特定角色天生就有（智慧勋章→薇克巴顿 `reader`、巧手勋章→薇诺娜 `basicengineer`、植物勋章→沃姆伍德 `plantkin`、暗影勋章→麦斯威尔 `magician`、童心勋章→沃尔特 `storyteller` 等）。
- **mod 只在真正新建时才记录管理**：组件/标签**已存在**（原生或能力勋章已加）→ mod 不干预、不记录 → 移除时天然不处理 → 原生能力保留。
- 反之，若 mod 在"组件已存在"时也记录（旧 bug），会踩坑：能力勋章把原生算成计数（装备2→卸下1），mod 移除时误把残留的原生计数减到 0 → **删掉原生组件**（薇克巴顿丢勋章后不能读书）。
- 本质与能力勋章 `medal_t_com`（临时组件标记）思路一致：**只管理自己新建的，不碰已有的**。
- 每次刷新末尾有汇总调试日志（`最终临时项=`），可核对最终状态与计数是否残留。

**佩戴/卸下刷新兜底（`equip_changed`）**：
- 检测带制作效果（标签/组件）勋章的佩戴状态变化（佩戴或卸下），变化则强制 `refreshcrafting`。
- 覆盖两个方向的漏刷：**佩戴**（直接进槽，临时项从未出现无增减）、**卸下**（一直佩戴，临时项为空无可清）。
- 首次调用（无上次记录）不强制，仅初始化状态。

### 2. 自动装备（`helper_autoequip.lua` + `helper_autoequip_util.lua` + `helper_autoequip_actions.lua`）

- 执行动作时自动佩戴对应勋章组最高级勋章（见第二节配置）。
- 一个动作可对应多组（`ACTION_TO_GROUP` 为数组），命中条件都装备，支持厨师/巧手共享 `BUILD/DISMANTLE/DEPLOY`。
- 仅服务端执行（装备需服务端权限）。
- **文件拆分**：`helper_autoequip_util.lua`（工具层，勋章查找/融合操作/目标条件匹配，经 `GLOBAL.AutoEquipUtil` 导出）；`helper_autoequip.lua`（核心层，动作映射/三层对比决策/装备组合/hook/时空守护）。加载顺序 `rules → actions → util → protect → autoequip`。

**条件字段扩展**：
- `props = { is_oversized = true }`：判断**目标属性**（非标签），如农场作物植株的 `is_oversized`。值为 `true` 表示目标须有该属性且为真；`false` 表示须无/为假。用于 `tags` 判断不到的属性型目标。
- `hand_tags = { "deployedfarmplant" }`：判断**手持物品（invobject）**带任一标签即整体通过（与目标条件为"或"关系），用于 DEPLOY 等目标为地面/空的动作判断手持物。

**按勋章分组（同一动作不同勋章分别限制）**：
- `action_targets` 的某个动作值可写成 `{ 勋章prefab = 条件表, ... }`，命中对应子条件则装组内该枚勋章（而非组内最优）。
- 识别方式：该表不含任何条件字段名（`tags`/`props`/`exclude_tags` 等）→ 视为按勋章分组；空表视为单个空条件。
- 例：`PICK = { transplant_certificate = { tags = {"thorny"} }, plant_certificate = { props = { is_oversized = true } } }` → 采带刺戴植物勋章、采巨型戴虫木勋章。

**指定勋章换装（`medalPrefab`）**：
- 每个 entry 都会算出一个**指定勋章 prefab**（`medal`）：配置里带 `medal` 的用它；未指定的（无 `medal` 字段的普通/单条件动作）在 `group_best` 阶段用 `FindBestGroupMedal` 算出组内最优勋章 prefab。`AutoEquipMedalForGroup` 统一用 `FindSpecificMedal` 装该指定勋章。
- 指定勋章时**强制换装**：当前戴的是指定勋章则跳过，否则强制换成指定勋章（如 PICK 采巨型时确实换上虫木勋章）。
- **指定勋章玩家没有时，不装备**（不回退到组内最优，避免考验/女武神条件命中却装了在该目标上无效的检验）。

**三层固定对比（`TryAutoEquip`，与有无融合勋章无关，普通动作与按勋章分组完全同一套流程）**：
1. **第一层（组内对比）**：每个命中的 entry，统一算出该组要装的勋章 prefab（配置带 `medal` 用指定，否则 `FindBestGroupMedal` 取组内最优），剔除未持有/无可装勋章，每组按 `MEDAL_LEVELS` 等级留一个**组内优胜者**（`group_best[group] = { entry, medal, rank }`）。未持有/不命中则不参与。
2. **第二层（同 prefab 对比）**：组内优胜者的指定勋章若有多个同 prefab 实例，由 `FindSpecificMedal` 处理——传入 `preferredFusion`（最优融合勋章）时**优先返回已在该融合勋章内的**（避免同 prefab 多个勋章来回换装）；否则优先已装备；低耐久偏好勋章（如逮捕）选耐久最低。
3. **第三层（跨组对比）**：配置了跨组优先级表的动作，收集**进入决赛**的组（在 `CROSS_GROUP_PRIORITY` 表内，按 `info.medal` 查优先级），按优先级**降序**排序：
   - **有融合勋章** → 按优先级**逐个装备**（能装几个装几个，都塞进融合勋章并存）。
   - **无融合勋章** → 勋章槽只有一个，**只装冠军**（优先级最高的组），避免逐个 `inv:Equip` 互相覆盖。
   - 无决赛选手（命中的组都不在优先级表）→ 回退逐组装备。

**跨组优先级（`CROSS_GROUP_PRIORITY`）**：
- 以**动作名**为 key，写死该动作下各勋章的优先级（数字越大越优先）。
- 只作用于"组与组"之间（进入决赛的组），不影响"主厨>大厨>烹饪"的同组关系（同组对比用 `MEDAL_LEVELS`）。
- **正义武神模式**：`CROSS_GROUP_PRIORITY.ATTACK` 里考验/检验默认 40（武神模式），UI「正义武神」选"正义"时动态降为 20（见 §9）。

**融合勋章满格多勋章避让（`usedSlots`/`FindFusionSlot`）**：
- 同一动作命中多组、都要塞进融合勋章时，`usedSlots` 记录本次已占用的格子，各勋章**避免竞争同一格**。
- `FindFusionSlot` 满格时**从后往前**兜底替换一个可放的格子（替代原 `GetSpecificMedalSlotForItem` 满格总返回最后一格的问题）。

**勋章保护机制（`helper_protect.lua` + `helper_autoequip_rules.lua` 的 `PROTECT_MEDALS`）**：
- 通用保护框架：特定环境下勋章不可被自动装备移走。每种保护勋章配置一个 `env(player)` 环境判定函数，返回 true 即进入保护。
- 配置在 `helper_autoequip_rules.lua` 的 `PROTECT_MEDALS`（纯数据+判定函数），新增保护勋章只需加一项，无需改逻辑。
- 内置判定函数（`GLOBAL.ComputeProtectedSet`）：计算玩家当前处于保护环境的受保护勋章真实 prefab 集合（`protectedSet`）。
- 接口：`GLOBAL.IsProtectedMedal`（是否登记保护）、`GLOBAL.IsMedalProtectedNow`（当前环境是否保护）、`GLOBAL.ComputeProtectedSet`（当前受保护集合）、`GLOBAL.GetEquippedProtectedMedal`（勋章槽当前受保护勋章）。
- 处理分支（`TryAutoEquip`，`protectedSet` 非空时）：
  - **无融合勋章 + 受保护勋章在勋章槽** → 不换装（return）。
  - **有融合勋章 + 受保护勋章在勋章槽** → 先把受保护勋章移入融合勋章，**再 `inv:Equip(fusion)` 把融合勋章装备回勋章槽**（确保勋章槽不空，玩家仍佩戴），再正常自动装备。⚠️注意：移入后必须装备融合勋章回勋章槽，否则勋章槽会空（如水面挖矿无矿工勋章时踏水勋章被移走导致勋章槽空）。
  - **受保护勋章在融合勋章内** → 正常自动装备，但 `FindFusionSlot` 跳过受保护勋章所在格子（不替换）。
- 当前已配保护勋章：
  - `treadwater_certificate`（踏水）：玩家在水面且不在船上（`GetCurrentPlatform()==nil` + `drownable:IsOverWater()`）时保护，防掉水淹死。
  - `ommateum_certificate`（复眼）：**非月圆夜晚**（`TheWorld.state.isnight and not isfullmoon`）或**风暴中**（`stormwatcher:GetCurrentStorm()>0` 且 `GetStormLevel()>SANDSTORM_FULL_LEVEL`(0.7)）时保护。判定对齐能力勋章 `functional_medal_defs.lua` 的复眼耐久消耗逻辑。
  - `down_filled_coat_certificate`（羽绒）：**秋冬**（`season=="autumn" or "winter"`）时保护（防冻）。
  - `blue_crystal_certificate`（蓝晶）：**春夏**（`season=="spring" or "summer"`）时保护（防过热）。

### 3. 制作栏重建优化（`helper_crafting_patch.lua`，客户端）

- 参考「降低客户端卡顿」模组：真正的卡顿来自 `RebuildRecipes` **全量重建**配方列表。
- **制作栏关闭**时：只轻量更新已固定（pinned）配方，不全量重建，避免卡顿；积压的刷新记入 `waittoupdate`。
- **打开制作栏**时：全量重建 + 补刷积压的刷新，清除关闭期间的状态残留。
- 仅跳过专用服务器（`IsDedicated`），**本地主机**也生效。
  > 注意：不能用 `TheNet:GetIsClient()` 判断——本地主机上它返回 `false` 会导致 patch 失效（实测踩坑）。
- 固定配方由 `CraftingMenuPinBar:RefreshPinnedRecipes` 收集，用单一最新 pinbar 实例（`ActivePinBar`）避免闭包累积内存泄漏。

### 4. 轮椅开关 UI 面板（`helper_medal_ui.lua` + `helper_medal_rpc.lua`，客户端）

**面板**（每玩家按组控制自动装备开关）：
- 打开方式：快捷键 / 暂停菜单按钮 / 控制台命令。
- 列出自动装备组（伐木/矿工/厨师/巧手/丰收/植物/智慧/速度时空/童真/暗影/淘气/钓鱼/浴火/触手/传承/正义/女武神◆）+ **正义武神**（左右箭头切换，默认武神）+ **攻击拦截**（默认关）+ **自动答题**（默认开）+ **奉纳透视**（默认关）开关。普通组「名字 + 左箭头(关) + 开/关状态 + 右箭头(开)」；正义武神「左箭头(正义) + 当前 + 右箭头(武神)」。
- **本源勋章不出现在 UI**：由机制控制（拥有即赋标签、自动装备），无 `group` 字段（`helper_rules.lua`），不受玩家开关控制。
- 打开时 `SetAutopaused(true)` 暂停游戏，ESC/点击背景 `SetAutopaused(false)` 恢复（须成对调用，计数增减）。
- 开/关箭头禁用态用 `Disable()/Enable()` 方法触发 `OnDisable` 切换禁用纹理（直接赋值 `enabled` 不刷新纹理）。
- 左右箭头（正义武神/自动补充）用统一 `MakeSpinnerArrow`（带禁用纹理），与普通开/关箭头一致。

**快捷键（方案 B：modinfo 全局默认 + 每玩家覆盖）**：
- `medal_ui_key`（modinfo 全局）为默认键；玩家可在 UI 右上角选择器覆盖存本地（`TheSim:SetPersistentString`）。
- 存储值：`"global"`(跟随全局) / `false`(关闭) / 数字键码。
- 改键后 `ReRegisterMedalKey` 用 `handler:Remove()` 解绑旧键、`AddKeyDownHandler` 注册新键。

**打字防误触**（参考「T键」模组）：`ToggleMedalUI` 开头检测 `IsTypingActive()`，以下情况不触发快捷键：
- 屏幕为 `ChatInputScreen`（聊天）/`ConsoleScreen`（控制台）/`DebugMenuScreen`（调试菜单）
- 当前屏幕有编辑框（`edit_text`）
- 引擎正强制处理文本（`forceProcessText`）
- 焦点控件链上有正在编辑的 `TextEdit`（如制作栏搜索框等自建输入框）

**暂停菜单入口**：`AddClassPostConstruct("screens/redux/pausescreen")` 加「轮椅开关」按钮，**仅当快捷键关闭时显示**（兜底）。

**控制台命令 `c_medalkey()`**：忘记快捷键时重置为关闭，使暂停菜单恢复入口。

**RPC 同步（`helper_medal_rpc.lua`）**：
- 客户端改开关后 `SyncGroupEnabled(json.encode(cfg))` 全量同步到服务端。
- **各开关同步方式**：勋章组开关 + 攻击拦截（`attackBlock`）都存 `group_enabled` 经 `SyncGroupEnabled`；强制保留 → `SyncForcedKeep`；自动补充阈值 → `SyncAutoRepair`；正义武神 → `SyncJVMode`（存 `player.medal_jv_mode`）。
- **RPC 参数用 json 字符串**：直接发 `boolean false` 会丢失（服务端收到 `nil`），json 能完整保留 `false`。
- 服务端 `player.medal_group_enabled` 更新后 `RefreshPlayerMedalTags` 刷新临时标签。
- **进服同步时机用 `playeractivated` 事件**（玩家激活后）：过早（`AddPlayerPostInit` 立即）会导致 RPC sender 是 `userid` 而非 `player`，报 `Invalid RPC sender`。
- 跨局存档验证：`TheSim:SetPersistentString` 本地持久化，重进游戏保持上次开关状态。

### 5. 蒙昧勋章自动答题（`helper_wisdom_autoexam.lua`，服务端）

- 佩戴蒙昧勋章（`wisdom_test_certificate`）时，自动装备新华字典（背包→手）并周期调 `MakeChoice(正确答案, player)` 跳过答题界面，直到字典耗尽或勋章升级。
- **开关**：UI 组开关 `autoexam`（默认开，`helper_medal_ui.lua` 有「自动答题」项）。
- **触发启动**：装备蒙昧勋章/新华字典；容器（融合勋章）收到蒙昧勋章且已生效；进服时已有生效蒙昧勋章。
- **停止**：蒙昧勋章不生效（卸下/丢失）；无字典或字典耗尽；勋章升级组件消失。
- **字典消耗**：用 `player.hasdictionary` 标志控制（对齐能力勋章 `medal_examable`），每本字典不漏用、耗尽自动停。

**答题防作弊（`helper_medal_exam_running`）**：
- 答题期间置 mod 内部标志，`helper_tags.lua` 将智慧勋章组视为关闭 → 不赋予临时 `reader` 组件 → 答对判定无 `reader` → 消耗按 **1 倍**（不翻倍）。
- **不碰 `medal_group_enabled`**（客户端权威值，服务端改会被客户端 RPC 同步覆盖）——之前踩坑：服务端改开关值会被本地值覆盖，故改内部标志。

### 6. 时空勋章·时空守护（已实现）

时空勋章（`space_time_certificate`）的被动保命能力。
- **触发**：玩家受到**致命伤害**时。
- **生效条件**：同时拥有 **本源勋章 + 时空勋章**，且**时空勋章耐久 ≥ 阈值** → 自动装备这两个勋章（保命）。
- **不生效**：两个勋章**只有一个**，或**时空勋章耐久 < 阈值** → 不自动装备。

**能力勋章源码已有"轮回"核心（`medal_hook.lua:3719-3746`，改 `health.SetVal`）**：
- **致命伤害判断**：`if val <= min_health and old_health > min_health`（即将降到死亡线以下，即"受致命伤"）。
- **触发前提**：`HasOriginMedal(self.inst, "spacetime_medal")`（本源勋章 + 时空勋章**已装备**）；再 `EquipMedalWithName("space_time_certificate")` 取时空勋章，`finiteuses:GetUses() >= REINCARNATION_CONSUME`。
- **效果**：消耗 `REINCARNATION_CONSUME` 耐久 → `currenthealth = min_health`（保命不死）→ 坠入时空塌陷表现（`medal_sinkhole`）→ `DoMedalReincarnationAnnounce` 公告 → `return` 跳过死亡。
- **参数**（`tuning_medal.lua:122`）：`REINCARNATION_CONSUME = ise and 200 or 300`（正常模式=300）。

**本 mod 实现**：能力勋章要求本源+时空勋章**已装备**才触发轮回。本 mod 在致命伤害时**先自动装备**这两个勋章（若拥有且时空耐久≥阈值），让 `HasOriginMedal` 成立，之后能力勋章轮回自然触发。

实现要点（`helper_autoequip.lua` + `helper_autoequip_actions.lua`）：
- **数据层**：`helper_autoequip_actions.lua` 的 `speedMedal` 组 `action_ids` 加入特殊动作 `"REINCARNATION"`（致命伤）。新增保命组只需在该组加此参数。
- **特殊动作识别**：`helper_autoequip.lua` 用 `SPECIAL_ACTIONS` 识别非 `ACTIONS` 动作（致命伤），`AddActionEntry` 命中时记录到 `SPECIAL_ACTION_ENTRIES["REINCARNATION"]`，不走 `ACTIONS` 查表。
- **阈值**：`REINCARNATION_CONSUME` 从 `GLOBAL.MedalAPI.TUNING_MEDAL.SPEED_MEDAL.REINCARNATION_CONSUME` 读取（对齐能力勋章），读不到回退 300。
- **致命伤监听**：`AddComponentPostInit("health")` hook 玩家 `health.SetVal`（对齐能力勋章，先于其轮回执行）。判断 `val <= min_health and old_health > min_health`（濒死）→ 调 `TryFatalDamageAutoEquip`。用 `inst.helper_fataldamage_hooked` 防重复 hook。
- **自动装备**：`TryFatalDamageAutoEquip` 遍历 `SPECIAL_ACTION_ENTRIES["REINCARNATION"]` 的组：须有本源勋章、组开关开启、组内最优勋章为**本源加成**勋章（时空勋章）且 `finiteuses:GetUses() >= REINCARNATION_CONSUME`，满足则 `AutoEquipMedalForGroup(inst, group, {action={id="REINCARNATION"}})`。
- **本源加成容器**：时空勋章在本源加成名单（`ORIGIN_MEDAL_BONUS`）。本源勋章是**最高级融合勋章**（`FusionMedals` 里 `origin_certificate` level=4），`FindBestFusionMedal` 按融合等级自然选中本源当容器装时空勋章，再装备本源勋章 → 本源给 `has_origin_medal`、时空给 `spacetime_medal`，`HasOriginMedal` 成立，轮回触发。（本源勋章无需额外"强制当容器"逻辑，有它自然最优先。）
- **决策缓存**：致命伤 action 用 `action_id="REINCARNATION"`、`target_prefab="none"`，复用现有决策缓存，已装备时静默。

### 7. 自动补充耐久（`helper_autorepair.lua`）

已装备勋章耐久低于玩家设定阈值时，自动用背包材料补充（能力勋章 `MedalAddUse`），仅服务端。
- **支持的勋章（`AUTOREPAIR_MEDALS`）**：踏水、羽绒、蓝晶、复眼、正义（新增只需在表里加一项）。
- **事件驱动**：普通勋章监听 `percentusedchange`（耐久变化）；正义勋章监听玩家 `onhitother`（攻击目标后补正义值，装备时挂/卸下移除）。
- **材料动态获取**：从勋章实例 `medal_repair_common` 动态取材料（如踏水=邪天翁羽毛、羽绒=鸭毛、蓝晶=蓝曜石、复眼=发光浆果/夜莓、正义=怪物肉/榴莲/怪物精华），无需硬编码。
- **阈值**：普通勋章 `关/10%/20%/30%`，默认 20%；正义勋章 `关/各目标/智能`，默认**智能**（存 `player.medal_autorepair={[prefab]=数值}`）。UI 里 `autoRepair` 标记项显示勋章名+阈值选择器。
- **避免浪费**：剩余缺口小于单个材料补量则不补（宁可不补满）。
- **2秒冷却**：`player._autorepair_cooldown`，避免耐久变化（含补充回涨）频繁触发。
- **玩家反查**：`GetGrandOwner()` 沿 owner 链反查（含融合勋章容器内勋章）。
- **在用判断**：直接佩戴 或 处于已装备的融合勋章内（DST 的 inventoryitem 无 container 字段，物品在容器里 owner 指向容器物品）。

**正义勋章自动补充正义值（对齐能力勋章）**：
- 正义值即 `finiteuses` 耐久。补充目标 = `JUSTICE_TARGETS` 表（15项）：`justice_targetlist` 的13目标（`consume` 5~50）+ 凋零之蜂（防控制固定5）+ 智能。
- **智能模式**（默认）：按攻击目标动态算所需正义值——`justice_targetlist` 目标查 `consume`；暗影生物（`norewardtoiler`）按 `gift_value*5`（本源减耗）；凋零之蜂固定5（不看本源）；普通怪物击杀增加正义值不补。
- **本源减耗**：有本源勋章时正义值需求 × `ORIGIN_MULT`(0.4) 再 ceil（凋零之蜂除外）。
- **触发时机**：`onhitother`（攻击命中后）。因能力勋章可能先判断正义值不足，**首次攻击该目标可能漏掉落**，补够后正常。
- 仅补**装备着**的正义勋章（攻击对应目标时自动装备）。

### 8. 强制保留勋章（`helper_medal_ui.lua` + `helper_medal_rpc.lua`）

自动装备时永久保留指定勋章不替换（如时空、本源等关键勋章）。
- **UI**：左上角"强制保留"按钮 → `ForcedKeepUIScreen`（`plantregistry.xml` 的 backdrop），上半部分勋章图标网格（10×2/页、翻页箭头），下半部分内置保留勋章（只读灰暗）。最多9个。
- **规则**：不重复、同组互斥（`GetMedalGroupOf` 优先能力勋章 grouptag）、排除内置/融合/空白（`blank_certificate`）/复制（`copy_blank_certificate`）勋章。
- **RPC**：`SyncForcedKeep` 服务端去重限9，存 `player.medal_forced_keep`。
- **效果**：`ComputeProtectedSet` 把强制保留勋章计入恒保护集合，自动装备时永不替换。

### 9. 正义武神模式（`helper_medal_ui.lua` + `helper_medal_rpc.lua` + `helper_autoequip.lua`）

控制**女武神组中级勋章（考验/检验）**与**正义组**在跨组决赛（`CROSS_GROUP_PRIORITY.ATTACK`）中的优先关系。
- **UI**：面板「正义武神」选项（左右箭头切换，默认**武神**）。选"正义"时左箭头禁用、选"武神"时右箭头禁用（对齐普通开/关箭头禁用纹理）。
- **持久化**：客户端存 `stored_data.jv_mode`（默认 `valkyrie`）；经 `SyncJVMode` 同步服务端存 `player.medal_jv_mode`。
- **生效**：`TryAutoEquip` 第三层跨组对比时，若玩家 `medal_jv_mode=="justice"`，用浅拷贝局部覆盖 `CROSS_GROUP_PRIORITY.ATTACK`，把考验/检验降为 20（不改全局表）。
- **效果**：
  - 默认**武神**：考验/检验跨组优先级 = 40（时空50 之下、逮捕/正义30 之上）→ 攻击怪物时女武神中级勋章优先于正义。
  - 选**正义**：考验/检验 = 20（与女武神20同级）→ 正义组（30）优先于女武神中级勋章。
- **注意**：该选项只影响 `CROSS_GROUP_PRIORITY.ATTACK` 里的考验/检验，不影响女武神最终勋章（恒为20）及其它动作。

### 10. 攻击拦截（`helper_attack_block.lua`，客户端）

针对**女武神勋章组（检验/考验）**：攻击目标与佩戴勋章不匹配（不满足升级条件）时，**在客户端禁止攻击指令**，防止升级失败。

**核心机制（规避"客户端读勋章不可靠"）**：
- **服务端**（`helper_rules.lua` + `helper_tags.lua`）：给检验/考验勋章配置**佩戴标签**（`medal_block_examine`/`medal_block_test`）。`helper_tags.lua` 在佩戴时（`tag_equipped`，含融合勋章内）给玩家加该标签并同步客户端，卸下时移除；**不配 group**（独立于女武神组开关，关闭女武神组不影响拦截）。
- **客户端**（`helper_attack_block.lua`）：读 `GLOBAL.GetStoredConfig()["attackBlock"]` 开关 + `player:HasTag` 佩戴标签 + `target:HasTag` 目标匹配，不匹配则拦截。

**客户端拦截 4 入口**（参考权威攻击拦截模组，`AddClassPostConstruct` 纯客户端 hook，覆盖所有攻击方式）：
1. `playercontroller.GetAttackTarget`（F键选怪）：拦截时返回 nil。
2. `playercontroller.DoAction`（动作执行最后防线）：拦截 ATTACK。
3. `playeractionpicker.GetLeftClickActions`（鼠标左键动作）：移除攻击动作。
4. `combat_replica.CanBeAttacked`（战斗副本，防其它途径）：返回 false 告知"目标不能打"。

**开关**：UI「攻击拦截」（默认**关**），存 `group_enabled["attackBlock"]`，客户端经 `GLOBAL.GetStoredConfig` 读取。

**匹配规则**（对齐 `helper_autoequip_actions.lua` ATTACK 条件）：
- 检验（佩戴标签 `medal_block_examine`）：`(largecreature+monster) 或 epic`。
- 考验（佩戴标签 `medal_block_test`）：`epic 或 monster` 且非 `smallcreature`。
- 女武神 `valkyrie_certificate`（最终级）：已满级，攻击不匹配目标不会降级，**不拦截**。

**实现要点**：
- 拦截日志已精简：拦截判断高频静默，实际拦截时不刷屏。
- `helper_medal_ui.lua` 导出 `GLOBAL.GetStoredConfig` 供客户端读开关。
