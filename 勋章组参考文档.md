# 能力勋章 · 勋章组参考文档

> 本文档根据「能力勋章」mod 源码（`能力勋章/scripts/medal_defs/functional_medal_defs.lua`）整理，供本 mod 开发对照使用。
> 更新于：2026-08-16

## 图例

- **组**：能力勋章里的 `grouptag`，同组勋章不能同时装备。
- **isfinal**：最终勋章（不再升级）。
- **升级/消耗方式**：该勋章通过什么行为消耗耐久、进而升级。
- **★ 已完成**：本 mod 自动装备已支持。

---

## 一、功能勋章组总览

### 1. chopMedal（伐木）★ 已完成

| prefab | 等级 | isfinal |
|---|---|---|
| `smallchop_certificate` | 初级 | 否 |
| `mediumchop_certificate` | 中级 | 否 |
| `largechop_certificate` | 高级 | 是 |

**升级方式（finishedwork 动作）**：
- **CHOP（砍树）**：目标**不含 `burnt` 标签**才消耗。默认 1；桦木精 20；万圣节活木树 6；正常活木树 10；有 `growable` 组件按 `fmod(stage,4)`（枯萎阶段不消耗）。
- **DIG（挖树根）**：目标**必须带 `stump` 标签**才消耗 1。
- 不消耗：CHOP 砍烧焦树；DIG 挖非树桩目标（地面/坟墓等）。
- 最终级 `largechop` 不消耗，改为触发天道酬勤。

**自动装备配置**：见第三节。

### 2. minerMedal（矿工）★ 已完成

| prefab | 等级 | isfinal |
|---|---|---|
| `smallminer_certificate` | 初级 | 否 |
| `mediumminer_certificate` | 中级 | 否 |
| `largeminer_certificate` | 高级 | 是 |

**升级方式（finishedwork 动作）**：
- **MINE（挖矿）**：**无条件消耗**（只要是 MINE 动作）。
  - 清理月雹（`islunarhail`）：消耗 2。
  - 按目标 prefab 查表：巨型月石/格罗姆雕像 10；盐矿 5；远古/大理石雕像 5；石化树 2；**石果 `rock_avocado_fruit` 消耗 0（不消耗）**；其余矿石默认 1。
  - 有 `growable` 组件按 `stage` 消耗。
- 不消耗：`rock_avocado_fruit`（石果）。
- 最终级 `largeminer` 不消耗，改为触发天道酬勤。

**自动装备配置**：见第三节。

### 3. chefMedal（烹饪/大厨）★ 已完成

| prefab | 等级 | isfinal |
|---|---|---|
| `cook_certificate` | 烹饪 | 否 |
| `chef_certificate` | 大厨 | 否 |
| `headchef_certificate` | 主厨 | 是 |

**升级方式**：非动作升级。
- `cook_certificate`：收获自己做的料理时消耗（大厨料理×2）。
- `chef_certificate`：收获料理消耗（大厨料理 5/8）；制作调味料消耗（上限 50）。
- `headchef_certificate`：不消耗。

**装备后标签（解锁能力）**：
- `cook_certificate`：`medal_cookbook`（食谱书）
- `chef_certificate`：`masterchef`、`professionalchef`、`expertchef` + `medal_cookbook`
- `headchef_certificate`：以上全部 + `seasoningchef`

**能力 → 动作对照**：

| 标签 | 能力 | 动作 | 整组/本源增强 |
|---|---|---|---|
| `seasoningchef` | 批量烤制 | `COOK` | 红晶锅整组烹饪需本源+主厨 |
| `masterchef` | 快速杀生 | `MURDER` | — |
| `seasoningchef` | 调味 | `CHEFFLAVOUR` | 整组调味需本源勋章 |
| `seasoningchef` | 研磨 | `MEDAL_GRINDING` | 整组研磨需时空勋章 |
| `seasoningchef`+本源 | 吃调料粉 | `MEDAL_EAT_SPICES` | — |
| `expertchef` | 强制冷却 | `MAKECOOLDOWN` | — |
| `seasoningchef` | 污染血糖 | `MEDALPOLLUTE` | — |
| `seasoningchef` | 制作专属配方 | `BUILD` | 便携设备=masterchef，调料=professionalchef，红晶锅=seasoningchef |

**专属厨具使用动作**：
- 打开便携烹饪锅/调料站：`RUMMAGE`
- 打开便携搅拌器：`OPEN_CRAFTING`
- 展开/收回便携设备：`DEPLOY` / `DISMANTLE`
- 从锅收获料理：`HARVEST`

**自动装备配置**：见第三节。

### 4. handyMedal（巧手）★ 已完成

| prefab | 等级 | isfinal |
|---|---|---|
| `handy_certificate` | 巧手 | 是 |
| `handy_test_certificate` | 巧手考验 | 否 |

**升级方式**：制作物品/建筑计数（`builditem`/`buildstructure`）。`handy_test_certificate`（巧手考验）`maxuses` 耐久用完 → `handy_certificate`（巧手，天道酬勤 RewardToiler）。

**装备后标签**：`handyperson`/`basicengineer`（女工科技）+ `has_handy_medal`（解锁专属配方制作）。

**专属配方制作**（`builder_tag="has_handy_medal"`，走 `BUILD` 动作）：造雨弹、放晴弹、时空弹、手摇深井泵改造装置、藏宝图、各种缪斯大理石雕像、混沌沙包等。

**自动装备配置**：见第三节。

> 注：巧手勋章组**无显式 grouptag**（`taglist` 仅 `addfunctional` 可入融合勋章），但 `handy_test_certificate`→`handy_certificate` 是完整升级链，与其它组结构一致。

### 5. 丰收勋章（`harvest_certificate`）★ 待新增自动装备

| prefab | 名称 | isfinal | 升级/消耗方式 |
|---|---|---|---|
| `harvest_certificate` | 丰收 | 是 | 不消耗，快采被动 |

- **标签**：佩戴时 `medal_fastpicker`（特殊快采）+ `quickpickmedal`。临时标签已配（`helper_rules.lua`）。
- **本源加成**：本源勋章可加成（采摘果树翻倍，`medal_fruit_tree.lua`）。
- **作弊风险**：`medal_fastpicker` 涉及采摘加速/晾肉架快采快干/本源果树翻倍，临时标签需谨慎，`helper_rules.lua` 已标注"待处理"。
- **自动装备**：单枚勋章（无 grouptag），待后续支持。

### 6. wisdomMedal（智慧）

| prefab | 等级 | isfinal |
|---|---|---|
| `wisdom_test_certificate` | 蒙昧考验 | 否 |
| `wisdom_certificate` | 智慧 | 是 |

**升级方式**：答题（`examable` 组件）消耗；答错反加耐久。

### 7. plantMedal（植物/虫木）

| prefab | 等级 | isfinal |
|---|---|---|
| `plant_certificate` | 虫木 | 否 |
| `transplant_certificate` | 植物 | 是 |

**升级方式**：采摘农场巨型作物（`picksomething` 事件）消耗。`transplant` 不消耗。

### 8. friendlyMedal（友善）

| prefab | 等级 | isfinal |
|---|---|---|
| `friendly_certificate` | 友善 | 否 |
| `bosom_friend_certificate` | 挚友 | 是 |

**升级方式**：获得友谊（`gainfriendship` 事件）消耗 1。攻击 pigman/bunnyman/catcoon（非 werepig/guard）会导致勋章消失。

### 9. justiceMedal（正义）

| prefab | 等级 | isfinal |
|---|---|---|
| `arrest_certificate` | 逮捕 | 否 |
| `justice_certificate` | 正义 | 是 |

**升级方式**：击杀（`medal_killed` 事件）。
- `arrest`：击杀 krampus 类消耗。
- `justice`：按目标表消耗正义值（krampus 5、klaus 50、bat 1、tentacle 5、worm 5、antlion/toadstool 50 等）；击杀 epic/monster 反而**增加**正义值。

### 10. speedMedal（速度/空间/时空）

| prefab | 等级 | isfinal |
|---|---|---|
| `speed_certificate` | 速度 | 否 |
| `space_certificate` | 空间 | 否 |
| `space_time_certificate` | 时空 | 是 |

**升级方式**：非动作升级。
- `speed`：无消耗，纯移速。
- `space`/`space_time`：通过传送塔传送消耗耐久。

### 11. valkyrieMedal（女武神）

| prefab | 等级 | isfinal |
|---|---|---|
| `valkyrie_examine_certificate` | 检验 | 否 |
| `valkyrie_test_certificate` | 考验 | 否 |
| `valkyrie_certificate` | 女武神 | 是 |

**升级方式**：击杀（`medal_killed`）。按目标血量扣耐久（≥4000 扣2，≥2000 扣1）；击杀小动物恢复 1。最终级不消耗。

### 12. spiderMedal（蜘蛛）

| prefab | 等级 | isfinal |
|---|---|---|
| `spider_certificate` | 蜘蛛 | 是 |

**升级方式**：**不消耗耐久**。纯被动（蜘蛛语、蜘蛛巢升级、蜘蛛网不减速）。

### 13. devoursoulMedal（噬魂）

| prefab | 等级 | isfinal |
|---|---|---|
| `devour_soul_certificate` | 噬灵 | 否 |
| `medium_devour_soul_certificate` | 噬魂 | 否 |
| `large_devour_soul_certificate` | 噬空 | 是 |

**升级方式**：灵魂跳跃（`medal_blink`）消耗。最终级可地图跳跃，灵魂不够时用耐久抵扣。

### 14. fishingMedal（钓鱼/渔翁）

| prefab | 等级 | isfinal |
|---|---|---|
| `smallfishing_certificate` | 钓鱼 | 否 |
| `mediumfishing_certificate` | 垂钓 | 否 |
| `largefishing_certificate` | 渔翁 | 是 |

**升级方式**：钓鱼事件（`medal_fishingcollect`）按池塘/鱼类表消耗。最终级不消耗。

### 15. childMedal（童心）

| prefab | 等级 | isfinal |
|---|---|---|
| `childishness_certificate` | 童心 | 否 |
| `childlike_certificate` | 童真 | 是 |

**升级方式**：射击事件（`medal_shoot`）按弹药表消耗（每种最多累计 60 次），沃尔特翻倍。最终级不消耗。

---

## 二、单枚勋章（无 grouptag）

| prefab | 名称 | isfinal | 升级/消耗方式 |
|---|---|---|---|
| `merm_certificate` | 鱼人 | 是 | 不消耗，鱼人标签 |
| `naughty_certificate` | 淘气 | 是 | 不消耗 |
| `bathingfire_certificate` | 浴火 | 是 | 不消耗，纵火/防燃 |
| `silence_certificate` | 沉默 | 是 | 不消耗 |
| `shadowmagic_certificate` | 暗影 | 是 | `upgradable` 升级；仆从死亡回耐久 |
| `down_filled_coat_certificate` | 羽绒服 | 是 | `fueled` 燃料持续消耗 |
| `blue_crystal_certificate` | 蓝晶 | 是 | `fueled` 燃料持续消耗 |
| `bee_king_certificate` | 蜂王 | 是 | `upgradable` 升级 |
| `ommateum_certificate` | 复眼 | 是 | `fueled` 燃料按时间消耗 |
| `treadwater_certificate` | 踏水 | 是 | `fueled` 燃料按移动消耗 |
| `tentacle_certificate` | 触手 | 是 | `upgradable` 升级 |
| `inherit_certificate` | 传承 | 是 | `upgradable` 升级 |

---

## 三、本 mod 自动装备已支持的勋章组

### chopMedal（伐木）

| 动作 | 条件 | 说明 |
|---|---|---|
| `CHOP` | 排除 `burnt` 标签 | 砍焦树不触发（能力勋章砍焦树不消耗） |
| `DIG` | 要求 `stump` 标签 | 仅挖树桩触发 |

### minerMedal（矿工）

| 动作 | 条件 | 说明 |
|---|---|---|
| `MINE` | 排除 `rock_avocado_fruit` | 挖石果不触发（能力勋章消耗0） |

### chefMedal（厨师）

**无条件动作（action_ids）**：

| 动作 | 依赖 | 备注 |
|---|---|---|
| `COOK` | seasoningchef | 批量烤制；红晶锅整组烹饪需本源+主厨 |
| `MURDER` | masterchef | 快速杀生 |
| `CHEFFLAVOUR` | seasoningchef | 调味，整组需本源勋章 |
| `MEDAL_GRINDING` | seasoningchef | 研磨，整组需时空勋章 |
| `MEDAL_EAT_SPICES` | seasoningchef+本源 | 吃调料粉 |
| `MAKECOOLDOWN` | expertchef | 强制冷却 |
| `MEDALPOLLUTE` | seasoningchef | 污染血糖 |

**带条件动作（action_targets）**：

| 动作 | 条件 | 说明 |
|---|---|---|
| `HARVEST` | `has_component=stewer` | 从锅收获料理（升级） |
| `RUMMAGE` | `prefabs=portablecookpot/portablespicer` | 打开便携烹饪锅/调料站 |
| `OPEN_CRAFTING` | `prefabs=portableblender` | 打开便携搅拌器 |
| `DEPLOY` | `prefabs=..._item` | 展开便携设备 |
| `DISMANTLE` | `prefabs=portablecookpot/blender/spicer` | 收回便携设备 |
| `BUILD` | `recipe_builder_tag=masterchef/professionalchef/seasoningchef` | 制作厨师专属配方 |

> **注意**：chefMedal 是通过事件/能力升级（收获料理），但有大量专属动作（调味/研磨/制作/使用厨具），因此自动装备覆盖了这些专属动作。
> 只有 `chopMedal` 和 `minerMedal` 是通过 `finishedwork`（挥砍/挖掘动作）消耗耐久的组。

### handyMedal（巧手）

**带条件动作（action_targets）**：

| 动作 | 条件 | 说明 |
|---|---|---|
| `BUILD` | `exclude_recipe_props=builder_tag`；`keep_recipe_builder_tag=handyperson/has_handy_medal` | 制作所有东西（快速制作），排除其他勋章专属配方，保留女工+巧手专属 |
| `UNWRAP` | `has_component=unwrappable` | 快速拆包 |
| `UNWRAPGIFTFRUIT` | `prefabs=medal_gift_fruit` | 拆包果（本源+巧手批量5个） |
| `UNWRAPOVERSIZEDGIFTFRUIT` | `all_tags=oversized_veggie+waxable` | 拆巨型包果（本源+巧手专属） |
| `DEPLOY` | `prefabs=winona_catapult_item/winona_spotlight_item/winona_battery_low_item/winona_battery_high_item` | 部署女工专属建筑（投石机/探照灯/发电机） |
| `DISMANTLE` | （暂未启用） | 拆除女工专属建筑 |

---

## 四、本 mod 核心机制

> 以下为 `scripts/` 下各模块的职责与关键逻辑，供开发维护对照。

### 1. 临时标签同步（`helper_tags.lua`）

- 拥有勋章但**未佩戴**时，临时赋予 `helper_rules.lua` 配置的标签/组件，使专属能力可用。
- **佩戴**后（含融合勋章内部），临时标签归能力勋章管，本 mod 只清自己的标记、不 `RemoveTag`，避免误删能力勋章真标签。
- 每次勋章状态变化（`itemget/itemlose/equip/unequip`）通过 `QueueMedalRefresh` **下一帧合并**（防抖）只刷新一次，避免连发事件频繁全量扫描。

**佩戴/卸下刷新兜底（`equip_changed`）**：
- 检测带制作效果（标签/组件）勋章的佩戴状态变化（佩戴或卸下），变化则强制 `refreshcrafting`。
- 覆盖两个方向的漏刷：**佩戴**（直接进槽，临时标签从未出现无增减）、**卸下**（一直佩戴，临时标签为空无可清）。
- 首次调用（无上次记录）不强制，仅初始化状态。

### 2. 自动装备（`helper_autoequip.lua` + `helper_autoequip_actions.lua`）

- 执行动作时自动佩戴对应勋章组最高级勋章（见第三节配置）。
- 一个动作可对应多组（`ACTION_TO_GROUP` 为数组），命中条件都装备，支持厨师/巧手共享 `BUILD/DISMANTLE/DEPLOY`。
- 仅服务端执行（装备需服务端权限）。

### 3. 制作栏重建优化（`helper_crafting_patch.lua`，客户端）

- 参考「降低客户端卡顿」模组：真正的卡顿来自 `RebuildRecipes` **全量重建**配方列表。
- **制作栏关闭**时：只轻量更新已固定（pinned）配方，不全量重建，避免卡顿；积压的刷新记入 `waittoupdate`。
- **打开制作栏**时：全量重建 + 补刷积压的刷新，清除关闭期间的状态残留。
- 仅跳过专用服务器（`IsDedicated`），**本地主机**也生效。
  > 注意：不能用 `TheNet:GetIsClient()` 判断——本地主机上它返回 `false` 会导致 patch 失效（实测踩坑）。
- 固定配方由 `CraftingMenuPinBar:RefreshPinnedRecipes` 收集，用单一最新 pinbar 实例（`ActivePinBar`）避免闭包累积内存泄漏。
